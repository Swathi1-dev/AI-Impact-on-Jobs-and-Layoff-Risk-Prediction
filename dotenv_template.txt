PROJECT_ROOT=PROJECT ROOT
DATASET="${PROJECT_ROOT}\DATASET PATH"
MODEL_DIR="${PROJECT_ROOT}\model dir PATH"
LOG_DIR="${PROJECT_ROOT}\log dir PATH"

TARGET_COL=Layoff_Risk
RANDOM_STATE=42

API_URL="API URL"
